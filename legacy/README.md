# Legacy — KRI-IP Crew Bash Scripts

These are the bash scripts that Archer replaces. They live in
`kri-ip/scripts/automation/` and remain active there until Archer's
Phase 1 is complete and validated.

Kept here as reference for:
- Behavior parity testing
- Edge cases discovered during the bash implementation
- The `install-crew.sh` launchd pattern (Archer uses APScheduler instead)

Do not modify these files — they are a snapshot, not the live versions.
